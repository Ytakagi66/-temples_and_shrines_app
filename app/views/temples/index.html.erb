<p id="notice"><%= notice %></p>

<h1>Temples</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Adress</th>
      <th>Nearest station</th>
      <th>Url</th>
      <th>Photo</th>
      <th>Location</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @temples.each do |temple| %>
      <tr>
        <td><%= temple.name %></td>
        <td><%= temple.adress %></td>
        <td><%= temple.nearest_station %></td>
        <td><%= temple.url %></td>
        <td><%= temple.photo %></td>
        <td><%= temple.location %></td>
        <td><%= temple.user_id %></td>
        <td><%= link_to 'Show', temple %></td>
        <td><%= link_to 'Edit', edit_temple_path(temple) %></td>
        <td><%= link_to 'Destroy', temple, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Temple', new_temple_path %>
