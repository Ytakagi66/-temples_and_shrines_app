<%= form_with(model: user_id, local: true) do |form| %>
  <% if user_id.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_id.errors.count, "error") %> prohibited this user_id from being saved:</h2>

      <ul>
      <% user_id.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :old %>
    <%= form.text_field :old %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
